'use strict';

/**
 * candidate-profile controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::candidate-profile.candidate-profile');
